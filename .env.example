# Optional — not required for the current story generator (ideas are local).
# Keep if you later add auth / cloud streak sync.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Never put service_role / secret keys in NEXT_PUBLIC_* or client code.
# If a secret key was exposed in a screenshot, rotate it in Supabase → API Keys.
